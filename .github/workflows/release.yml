name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  # TODO: Implement CI for Node.js/npm + Electron build pipeline
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow not yet implemented"
