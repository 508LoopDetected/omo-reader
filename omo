#!/usr/bin/env bash
# Thin wrapper so `./omo setup` / `./omo deploy` work from the repo root
# without a separate build step. Uses tsx (already a workspace devDep).
set -euo pipefail
cd "$(dirname "$0")"
exec ./node_modules/.bin/tsx packages/cli/src/index.ts "$@"
